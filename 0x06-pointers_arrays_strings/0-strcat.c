#include "main.h"

/**
 * _strcat - function for concatenating strings
 * @dest: pointer
 * @src: append to dest
 * return: pointer to @dest
 */
char *_strcat(char *dest, char *src)
{ 
	char src;
	char dest;

	strncat(dest, src);

	return 0;
}

