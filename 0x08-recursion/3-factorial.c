#include "main.h"

/**
 * factorial - function returns factorial
 * @n: number
 * Return: factorial
 */
int factorial(int n)
{
	int n;
	
	if (n <= 0)
	       return (1);
	return (n * factorial(n - 1);
}

